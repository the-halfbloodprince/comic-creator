import React, { FC } from 'react'

type Props = {}

const ComicStripTemplate2: FC<Props> = () => {
  return (
    <div>This is a dummy template component. It has been kept just to present the idea of how users may want different templates and hence can choose between those. In practicality, users must be able to resize and modify the grid / comic strip themselves but implementation of that is not attempted in this task.</div>
  )
}

export default ComicStripTemplate2